timeout: 40m
options:
  machineType: 'N1_HIGHCPU_8'
steps:
- id: 'apiserver'
  name: 'docker'
  timeout: 10m
  waitFor: ["-"]
  args: [
    'build',
    '--tag',
    'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/apiserver:${COMMIT_SHA}',
    '--build-arg',
    'SERVICE=apiserver',
    '.',
  ]
- id: 'cleanup'
  name: 'docker'
  timeout: 10m
  waitFor: ["-"]
  args: [
    'build',
    '--tag',
    'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/cleanup:${COMMIT_SHA}',
    '--build-arg',
    'SERVICE=cleanup',
    '.',
  ]
- id: 'server'
  name: 'docker'
  timeout: 10m
  waitFor: ["-"]
  args: [
    'build',
    '--tag',
    'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/server:${COMMIT_SHA}',
    '--build-arg',
    'SERVICE=server',
    '.',
  ]
- id: 'adminapi'
  name: 'docker'
  timeout: 10m
  waitFor: ["-"]
  args: [
    'build',
    '--tag',
    'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/adminapi:${COMMIT_SHA}',
    '--build-arg',
    'SERVICE=adminapi',
    '.',
  ]
- id: 'deploy'
  waitFor:
  - 'adminapi'
  - 'apiserver'
  - 'cleanup'
  - 'server'
  name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:303.0.0-alpine'
  args:
  - 'bash'
  - '-eEuo'
  - 'pipefail'
  - '-c'
  - |-
    for _SERVICE in ( adminapi apiserver cleanup server ); do
      gcloud run deploy "${_SERVICE}" \
        --quiet \
        --project "${PROJECT_ID}" \
        --platform "managed" \
        --region "${_REGION}" \
        --image "gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/${_SERVICE}:${COMMIT_SHA}" \
        --no-traffic
    done
images: [
  'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/server:${COMMIT_SHA}',
  'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/apiserver:${COMMIT_SHA}',
  'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/cleanup:${COMMIT_SHA}',
  'gcr.io/${PROJECT_ID}/github.com/google/exposure-notifications-verification-server/cmd/adminapi:${COMMIT_SHA}',
]
